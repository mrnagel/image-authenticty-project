services:
  trufor:
    build: 
      context: ../models/trufor
      dockerfile: Dockerfile
    image: trufor
    volumes:
      - ./model_outputs:/model_outputs
      - ./saved_photo:/saved_photo

  bfree:
    build: 
      context: ../models/bfree
      dockerfile: Dockerfile
    image: bfree
    volumes:
      - ./model_outputs:/model_outputs
      - ./saved_photo:/saved_photo