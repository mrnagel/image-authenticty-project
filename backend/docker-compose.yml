services:
  trufor:
    build: 
      context: ../models/trufor
      dockerfile: Dockerfile
    image: trufor
    volumes:
      - ./model_outputs/trufor:/model_outputs
      - ./saved_photo:/saved_photo