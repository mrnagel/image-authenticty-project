import { Component } from '@angular/core';
import { Upload } from '../upload/upload';

@Component({
  selector: 'app-home',
  imports: [Upload],
  templateUrl: './home.html',
  styleUrl: './home.scss',
})
export class Home {

}
